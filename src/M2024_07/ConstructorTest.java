package M2024_07;

/**
 * 기본 생성자, 생성자 선언, 매개 변수, 객체 초기화, 오버로딩, this()
 * 
 * @author PigYoung90 생성자는 new 연산자로 호출되는 중괄호{} 블록이다. 객체 생성 시 초기화를 담당한다.
 * 
 */
public class ConstructorTest {

	/*
	 * 생성자 (constructor) 클래스로부터 new 연산자로 객체를 생성할 떄 호출되어 객체의 초기화를 담당 객체 초기화 필드를
	 * 초기화하거나 메소드를 호출해서 객체를 사용할 준비를 하는 것 생성자가 성공적으로 실행 힙 영역에 객체 생성되고 객체 번지가 리턴
	 */

	/*
	 * 기본 생성자 (default constructor) 클래스 내부에 생성자 선언 생략할 경우 바이트 코드에 자동 추가
	 * 
	 * [public] class(){}
	 */

}
/*
public class Car{
	
	public Car() {} ->  여기서의 Car()는 기본생성자 // 컴파일시 바이트 코드 파일에서 자동 추가된다.
	
	Car myCar = new Car(); -> 여기서의  Car()는 기본생성자 이다. 
}

*/
