package M2024_06;

public class Servlet_Study {
	/*
	 * 서블릿은 서버 쪽에서 실행되면서 클라이언트의 요청에 따라 동적으로 서비스를 제공하는 자바 클래스입니다. 서블릿은 자바로 작성되어 있으므로
	 * 자바의 일반적인 특징을 모두 가집니다. 하지만 서블릿은 일반 자바 프로그램과 다르게 독자적으로 실행되지 못하고 톰캣과 같은 JSP /
	 * Servlet 컨테이너에서 실행된다는 점에서 차이가 있습니다.
	 * 
	 */

}
